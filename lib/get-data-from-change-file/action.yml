name: 'Get data from change file'
description: 'Returns data from a changefile'
inputs:
  changedFiles:
    description: 'An array of files that have been changed or added'
    required: true
outputs:
  incrementation:
    description: 'The level of incrementation that should be completed.'

runs:
  using: 'node12'
  main: 'index.js'
